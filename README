-*- mode: outline -*-

This directory tree holds 3 Emacs packages:
 - The geo-xdg Lisp library.
 - The geo Lisp library.
 - The geo-xdg-solar Lisp package.

* geo-xdg.el
`geo-xdg' provides location information to GNU Emacs, using the XDG GeoClue API.
`geo-xdg-solar' provides location information to calendar.el, using `geo-xdg'.

Some documentation is provided inside the docstrings, and a manual is also available.

* geo.el
  `geo' is a generic interface to location information for GNU Emacs.
